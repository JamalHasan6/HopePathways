# name: 🚀 HopePathways — Backend Deploy

# on:
#   push:
#     branches:
#       - azure-deploy
#   workflow_dispatch:

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read #This is required for actions/checkout

#     steps:
#       - uses: actions/checkout@v4

#       - name: Set up Node.js version
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20.x'

#       - name: Install dependencies
#         working-directory: backend
#         run: npm ci

#       - name: Build backend
#         working-directory: backend
#         run: npm run build

#       - name: Upload artifact for deployment job
#         uses: actions/upload-artifact@v4
#         with:
#           name: backend-app
#           path: backend

#   deploy:
#     runs-on: ubuntu-latest
#     needs: build
#     permissions:
#       id-token: write
#       contents: read

#     steps:
#       - name: Download artifact from build job
#         uses: actions/download-artifact@v4
#         with:
#           name: backend-app
#           path: backend

#       - name: Validate Azure credentials secret
#         run: |
#           if [ -z "${{ secrets.AZUREAPPSERVICE_CLIENTID_216A23E07ECE46348F25F4C94280CC6D }}" ]; then
#             echo "AZUREAPPSERVICE_CLIENTID_216A23E07ECE46348F25F4C94280CC6D is missing."
#             exit 1
#           fi

#       - name: Login to Azure
#         uses: azure/login@v2
#         with:
#           client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_216A23E07ECE46348F25F4C94280CC6D }}
#           tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_E06CC835EF014ADB99E160BA7B0821C7 }}
#           subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_AB4908EC07184F0991DB2121D5E43364 }}

#       - name: Verify Azure login
#         run: az account show

#       - name: Create backend deployment archive
#         run: |
#           cd backend
#           zip -r ../backend.zip . -x ".git/*"

#       - name: Deploy to Azure Web App via Azure CLI
#         run: |
#           az webapp config set \
#             --resource-group "ngm-hackathon-team-8-rg" \
#             --name hpb \
#             --startup-file "node dist/index.js"
#           az webapp deploy \
#             --resource-group "ngm-hackathon-team-8-rg" \
#             --name hpb \
#             --src-path backend.zip \
#             --type zip
          